<UserControl x:Class="EmploymentAgency.Views.UserControls.ExpanderWithCheckBoxes"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006" 
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
             xmlns:mdt="http://materialdesigninxaml.net/winfx/xaml/themes"
             xmlns:local="clr-namespace:EmploymentAgency.Views.UserControls"
             x:Name="This"
             mc:Ignorable="d" 
             d:DesignHeight="200" d:DesignWidth="200">
    <Grid>
        <Expander Background="#673AB7"
                  HorizontalAlignment="Stretch"
                  MaxHeight="400">
            <Expander.Header>
                <TextBlock Text="{Binding ElementName=This, Path=ElementHeader, UpdateSourceTrigger=PropertyChanged, Mode=TwoWay}"
                           Padding="0"
                           Foreground="White"
                           FontSize="20"
                           FontWeight="Medium"/>
            </Expander.Header>
            <ScrollViewer HorizontalScrollBarVisibility="Visible" ScrollChanged="ScrollViewer_ScrollChanged">
                <Grid Name="Grid" Background="#FFE5E5E5"/>
            </ScrollViewer>
        </Expander>
    </Grid>
</UserControl>
